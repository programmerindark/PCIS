"""SQLite (initially; PostgreSQL later, per project plan) persistence
for house configurations, flock records, and recommendation history.

See `models.py` for the schema and `session.py` for engine/session
setup and the save/query helper functions.
"""
